指令：在上一步基础上改成交互式—— 
 用户输入本金/利率/年限，程序输出结果； 
 输入负数利率时提示"利率不能为负"并要求重输。

# -*- coding: utf-8 -*-
# 交互式复利计算程序
#
# 复利公式（每年复利 1 次）：A = P * (1 + r) ** t
#   A —— 最终本息和
#   P —— 本金
#   r —— 年利率（小数）
#   t —— 存款年数
# ---------- 输入本金 ----------
# input() 读取用户输入，得到的是字符串（带引号的文本）
# float() 把字符串转成小数，例如 "10000" -> 10000.0
P = float(input("请输入本金（元）："))
# ---------- 输入年利率 ----------
# 用 while True 循环：一直要求用户输入，直到输入合法才用 break 跳出循环
while True:
    rate_percent = float(input("请输入年利率（%，如 5 表示 5%）："))  # 用户输入百分数，如 5
    if rate_percent < 0:                 # 判断利率是否为负
        print("利率不能为负")            # 提示错误信息
        continue                         # 回到循环开头，要求重新输入
    break                                # 利率合法，跳出循环
r = rate_percent / 100  # 百分数换成小数，5 -> 0.05
# ---------- 输入存款年限 ----------
t = int(input("请输入存款年限（年）："))

# ---------- 计算并输出结果 ----------
# (1 + r)：1 元存 1 年后变成 1.05 元；** t：连存 t 年（1.05 的 t 次方）
# 再乘以本金 P，得到最终本息和
A = P * (1 + r) ** t
# :.2f 保留两位小数
print("最终本息和为：{:.2f} 元".format(A))
